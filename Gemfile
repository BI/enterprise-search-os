source 'http://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~>4.2.0'

gem 'mime-types', '1.24' # 1.25 seems to have installation problems

# Use SCSS for stylesheets
gem 'sass-rails'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails'


gem 'jquery-rails'
gem 'jquery-ui-rails'

#gem 'jruby-openssl'
#gem 'yajl-ruby'
gem 'json', '~> 1.8.0'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
#gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
#gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
gem 'bcrypt-ruby'

# Views
gem 'kaminari'
gem 'slim'

gem 'settingslogic'
gem 'request_store'

gem 'rest-client'
gem 'redis'
gem 'elastics-rails'
gem 'elastics-admin'

gem 'anemone', :require => false # required by the crawler
gem 'peach'                      # parallel each used in the remote_indexer

gem 'mysql2'

gem 'nokogiri'


